# -*- mode: snippet -*-
# name: begin_src
# key: src
# --
#+begin_src ${1:lang}
${2:`(if (region-active-p) (yas-selected-text) (propertize "content" 'font-lock-face '(:inherit region)))`}
#+end_src
$0